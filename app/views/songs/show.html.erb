<h1><%= @song.name %></h1>
<% @artist = Artist.find(@song.artist_id) %>
<% @genre = Genre.find(@song.genre_id) %>
<%= link_to @artist.name, artist_path(@artist) if @artist %><br>
<%= link_to @genre.name, genre_path(@genre) if @genre %><br>
